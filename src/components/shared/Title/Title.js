import React from 'react';
import './title.scss';

const Title = ({ text }) => {
    return (
        <div className="title-component">
            {text}
        </div>
     );
}
 
export default Title;